# TUI PR list slice

Objective: after the owner verified page navigation, make the Pull Requests page useful with current-repository open PRs, explicit linked-task identities and read-only delivery observations. Continue one slice at a time; no task-row PR column, GitHub writes, workflow drill-down, Project queue source configuration, persistent cache or merge is included.

Authority: the user authorized this slice with Continue. The existing TASK-80 contract remains the authority for Project queue integration; this repository-scoped PR view is a separate read surface and does not create Project bindings or shadow tasks. Existing recorded canonical references associate tasks to observed PRs. Titles, branch names and incidental prose never establish linkage. Incomplete/unknown evidence must not appear as reassurance.

Sequence: core bounded read adapter; TUI asynchronous loading and list/detail; E2E navigation/failure plus real GitHub read proof; crate gates; commit/install for owner review. Root owns TUI/integration/docs; pr_adapter owns core adapter and its tests. Initial feat/tui-pages worktree status was clean at 9251683. Primary-checkout baseline dirt remains untouched.

Acceptance: visible source repo and observation freshness; PR identity and linked IDs; checks/review/merge observations without overall success inference; bounded loading off the render path; explicit empty/error/stale/partial states; preserve task context and task bytes; no hidden task mutations; install checked build. Evidence and gaps will be recorded after verification. Stall thresholds, workflow history, archived-reference completeness and human approval remain separate concerns.

## Verification and remaining scope

Core: 10 pr_list tests passed, including malformed/empty/partial data, conservative check results, identity and bounded subprocess handling; core Clippy passed. TUI: mise run tui passed formatting, Clippy and all ordinary E2E journeys. Two explicitly invoked live E2Es read the real budget repository, displayed PR #958, then used a temporary real git/backlog fixture with a canonical reference to prove the linked task cell and failed-refresh stale retention. Byte comparisons prove no task changes from reading. Captures and live results are in /tmp/switchbard-pr-list-live-evidence.log; crate output is in /tmp/switchbard-pr-list-tui-checks.log (ephemeral local evidence, not committed private PR content).

State/stress: real-key tests cover loading, unavailable non-repo, retry, page changes during fetch, no hidden task mutation, preserved filters/selection, populated live list/detail, empty task lists and terminal sizes. Live link/stale journeys cover 80x24, 120x40, 180x50 and 40x12 including scrolling detail to its final paragraph. Core synthetic tests cover max-row truncation and uncertain check states. High-volume native visual inspection, true authentication/rate-limit fault injection and Linux live GitHub use are not proven here. No overall progress/ready verdict, required-check coverage, archive/prose association completeness, GitHub writes or Project queue integration is claimed. Native owner review remains pending installation.

The existing pinned ratatui 0.29 rendered-line-info feature is enabled so detail scrolling uses the same wrapping calculation as rendering rather than duplicating it. Links are recalculated on task or snapshot change, not each frame. PR selection follows opaque identity; if the selected PR leaves the open list, detail closes with an explicit message instead of silently presenting another PR.

Install: mise run tui-install passed and replaced /Users/bpc/.cargo/bin/sbt with code revision 4af9a25. TASK-160 is In Review for owner use. Changes committed locally on feat/tui-pages; no push, merge or full-workspace CI. The current implementation outcome is delivered; human approval and the later feature slices remain open.

## Owner correction: all-state source, filters over observations

The owner explicitly rejected restricting the dataset to active PRs. The current slice supersedes that earlier source choice: fetch all PR lifecycle states, start unfiltered, and apply status filters over the cached dataset with the existing task filter grammar/picker interactions. PR filtering stays separate from task filtering and survives self-restart. Fetch the first 100, allow :more to raise the request by 100 up to the explicit 1000-PR cap; filtering a partial dataset never proves no matching PR exists beyond it. All-state metadata loads independently from optional active-PR enrichment so historical check queries cannot block seeing the list. Persistent saved PR views, sorting/grouping/column customization are subsequent organization work, not claimed as complete parity here.

Initial worktree was clean at 94b3a6e. The first live all-state gate failed: heavy historical-rollup reads returned a GitHub JSON error and failed expansion to 200. The repair separates bounded metadata and active enrichment; success requires rerunning the real history gate, not accepting retained stale data as a successful expansion.

The owner additionally requested Enter to open details on the right like Tasks. The list remains visible; j/k navigate rows with the pane open, Ctrl-d/u scroll wrapped detail, and Enter/Esc close it. Shared filter picker and restart journeys pass; three serialized live GitHub E2Es now pass after the metadata repair, including all lifecycle states, local filtering without refetch, expansion from 100 to 200, canonical task links, retained stale observations, unchanged task bytes and detail scrolling at 40x12 through 180x50. Core adapter tests (15) and core Clippy pass. Native owner approval, Linux live reads, rate-limit/auth fault injection and full sorting/grouping/saved-view parity remain explicit gaps.

Install closeout: code revision d1326d2 passed mise run tui-install and replaced /Users/bpc/.cargo/bin/sbt. The additional live j/k journey confirmed selection changes while the right pane remains open. Logs: /tmp/switchbard-pr-all-states-live.log, /tmp/switchbard-pr-pane-navigation.log and /tmp/switchbard-pr-all-states-install.log. TASK-161 is In Review; native owner acceptance remains pending. Work is committed on feat/tui-pages, with no push or merge; full-workspace CI is deferred to the merge gate per the crate workflow.
