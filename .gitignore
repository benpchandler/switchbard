# Build outputs
target/
*.app/
*.dSYM/
*.dmg
*.sha256

# Local planning / scratch space
docs/plans/
tmp/
.cache/

# macOS / Finder noise
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes

# Editor / IDE local state
.idea/
.vscode/
*.swp
*.swo
*~

# Local env / secrets
.env
.env.*
!.env.example

# Logs
*.log
logs/

# Python cache noise
__pycache__/

# Local-only Claude Code hook (rebuild DMG + reload app on change) — personal,
# not shared. See .claude/settings.local.json for the binding.
.claude/settings.local.json
/scripts/rebuild-and-reload.sh

# Perf smoke raw captures; keep durable summaries in docs/perf/runs/.
docs/perf/raw/
docs/perf/**/*.csv

# Rust coverage / profiling artifacts
*.profraw
*.profdata
coverage/
tarpaulin-report.html
lcov.info

# UI snapshot transient outputs; keep committed baselines trackable
**/tests/snapshots/**/*.diff.png
**/tests/snapshots/**/*.new.png
**/tests/snapshots/*.diff.png
**/tests/snapshots/*.new.png
*.snap.new
