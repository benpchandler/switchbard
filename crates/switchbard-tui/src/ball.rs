//! The ball column reads `switchbard_core::Ball`; the label vocabulary and
//! the `b`-key cycle live there so `sb edit --ball` and this TUI can never
//! disagree about what the labels are called.

pub use switchbard_core::Ball;
